# 2026-06-23 LangWIKI 前端美化初始化

## 任务
用户要求初始化前端美化的设计上下文（`/impeccable init`）。

## 完成内容
1. **创建设计上下文文档** `frontend/DESIGN_CONTEXT.md`
   - 现状审计（技术栈、11 个页面、视觉问题）
   - 设计目标：知识工程师工作台质感
   - 完整设计令牌：品牌色 9 阶、surface 色板、字体、间距、圆角、阴影、动效
   - 组件规范：Button(5变体)、Card、Input、Badge、StatusDot、Table
   - 布局规范：侧边栏 280px、PageHeader、响应式断点
   - 交互规范：加载态(骨架屏)、空状态、Toast、过渡动效
   - 图标方案：lucide-react
   - 4 阶段美化路线图

2. **扩展 tailwind.config.js**
   - brand 色 50-900 完整 9 阶
   - surface 色板（base/card/sidebar）
   - 自定义字体栈（含 PingFang SC 中文）
   - 自定义圆角 sm/md/lg
   - 自定义阴影 card/card-hover/popover
   - transitionDuration fast/normal/slow
   - 动画 fade-in / pulse-dot

3. **重写 index.css**
   - base 层：字体平滑、滚动条美化
   - components 层：.btn(5变体3尺寸)、.card、.input、.badge(5语义)、.status-dot、.page-header、.skeleton
   - 所有组件含完整四态(hover/active/focus/disabled)

4. **建立 UI 组件层** `src/components/ui/`
   - Button.jsx、Card.jsx、Input.jsx、Badge.jsx
   - StatusDot.jsx、PageHeader.jsx、Skeleton.jsx、EmptyState.jsx
   - index.js 统一导出

5. **安装 lucide-react** 图标库

## 构建验证
`npx vite build` 通过，CSS 22KB / JS 203KB。

## 关键决策
- 不引入完整 UI 框架（如 Ant Design / shadcn），保持轻量，用 Tailwind 组件类自建
- 设计令牌走 Tailwind extend + CSS @layer components 双层落地
- 后续页面美化只使用 components/ui 中的组件，不直接堆砌原子类

## Phase 2：骨架美化（已完成）
1. **Toast 全局反馈系统** `components/ui/Toast.jsx`
   - ToastProvider + useToast hook，注入 main.jsx
   - success(3s自动消失) / error(需手动关闭) / info 三种
   - 右上角浮层，lucide 图标 + 左边框语义色
2. **ShellLayout 重构**
   - 侧边栏 w-72(288px)、主区 max-w-6xl 居中、p-8
   - 路由切换 fade-in 动效（key=pathname 触发重挂载）
3. **Sidebar 重构**
   - lucide 图标系统替换文本符号（Layers/Plus/ChevronDown/Right/Settings/Folder/FileText）
   - 工作区项 hover 时设置按钮渐显（opacity-0 group-hover:opacity-100）
   - 文件树缩进优化（depth * 12px）、空工作区提示
   - 用户头像改 brand-600 底色

## Phase 3：核心页面重做（已完成）
1. **Dashboard**：状态概览条 + 进度条(三色) + 4 个 StatCard + 骨架屏 + 刷新按钮
2. **Query**：PageHeader + 卡片输入 + ⌘Enter 快捷键 + 骨架屏 + 证据卡片 + 空状态
3. **Ingest**：PageHeader + 状态徽章 + 概览卡 + 分组操作卡(批量/实体/队列) + toast + 目录浏览器
4. **Workspace**：分组卡片表单(基础/Git) + toast + 目录浏览器 + 空状态

## 构建验证
最终 `npx vite build` 通过，CSS 26.7KB / JS 232KB。lint 零错误。dev server 运行在 5173。

## Phase 3 补充：剩余 7 页面重做（已完成）
5. **Entity**：PageHeader + 返回按钮 + 骨架屏 + Wiki 卡片 + 空状态
6. **Schema**：PageHeader + Schema 编辑卡(monospace) + 建议列表卡(采纳/忽略) + toast + 骨架屏
7. **Profile**：PageHeader + 用户标识卡 + 上下文/Wiki 双栏(只读 Badge) + AutoBlocks JSON 编辑卡 + toast
8. **UserSettings**：PageHeader + 底边线 Tab(带图标) + 基本设置卡 + toast
9. **LlmSettings**：PageHeader + 模型选择卡(select+Badge) + 温度滑块卡 + API Key 管理卡 + 测试结果卡 + toast
10. **WorkspaceSettings**：PageHeader + 底边线 Tab(4个带图标，横向滚动) + 复用子页面
11. **WorkspaceDoc**：PageHeader + 返回按钮 + 文件路径 + 骨架屏 + 错误状态 + 空状态

## Tab 组件统一
所有 Tab 页面（UserSettings / WorkspaceSettings）统一采用底边线样式：
- active: border-brand-600 + text-brand-700 + font-medium
- inactive: border-transparent + text-slate-600 + hover:text-slate-900
- 带 lucide 图标

## 关键决策
- 不引入完整 UI 框架，用 Tailwind 组件类自建
- 设计令牌走 Tailwind extend + CSS @layer components 双层落地
- 业务页面只用 components/ui 组件，不堆砌原子类
- 所有页面反馈从裸 message 文本升级为 Toast 系统
- 所有页面统一 PageHeader + Card 组件，消除散落的 h1 和裸 div 卡片

## 全部 11 个页面美化完成，Phase 1-3 全部落地。

## Phase 4：响应式适配（已完成）
1. **useMediaQuery hook** `hooks/useMediaQuery.js` — 监听 window.matchMedia 变化
2. **ShellLayout 响应式改造**
   - 桌面端(≥1024px)：侧边栏固定显示
   - 移动端(<1024px)：顶栏(汉堡菜单+Logo) + 抽屉式侧边栏 + 黑色遮罩层
   - 路由切换自动关闭抽屉
   - 主区内边距响应式：p-4(移动) / md:p-6 / lg:p-8
3. **Sidebar 适配** — min-h-screen → h-screen，适配 fixed 容器
4. **PageHeader 响应式** — 移动端标题+操作垂直堆叠(flex-col sm:flex-row)，标题 truncate
5. **page-title 字号响应式** — text-xl(移动) lg:text-2xl(桌面)
6. **触摸优化** — body 添加 -webkit-tap-highlight-color: transparent
7. **各页面 grid 已内置响应式** — Dashboard grid-cols-2 lg:grid-cols-4，双栏页 lg:grid-cols-2

构建：CSS 27.9KB / JS 234KB，lint 零错误。

## 关键决策
- 不引入完整 UI 框架，用 Tailwind 组件类自建
- 设计令牌走 Tailwind extend + CSS @layer components 双层落地
- 业务页面只用 components/ui 组件，不堆砌原子类
- 所有页面反馈从裸 message 文本升级为 Toast 系统
- 所有页面统一 PageHeader + Card 组件，消除散落的 h1 和裸 div 卡片
- 响应式断点：1024px（侧边栏固定/抽屉切换）

## Phase 1-4 全部完成。暗色模式作为可选项暂未实现。
