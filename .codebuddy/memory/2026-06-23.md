# 2026-06-23 LangWIKI 前端美化初始化

## 任务
用户要求初始化前端美化的设计上下文（`/impeccable init`）。

## 完成内容
1. **创建设计上下文文档** `frontend/DESIGN_CONTEXT.md`
   - 现状审计（技术栈、11 个页面、视觉问题）
   - 设计目标：知识工程师工作台质感
   - 完整设计令牌：品牌色 9 阶、surface 色板、字体、间距、圆角、阴影、动效
   - 组件规范：Button(5变体)、Card、Input、Badge、StatusDot、Table
   - 布局规范：侧边栏 280px、PageHeader、响应式断点
   - 交互规范：加载态(骨架屏)、空状态、Toast、过渡动效
   - 图标方案：lucide-react
   - 4 阶段美化路线图

2. **扩展 tailwind.config.js**
   - brand 色 50-900 完整 9 阶
   - surface 色板（base/card/sidebar）
   - 自定义字体栈（含 PingFang SC 中文）
   - 自定义圆角 sm/md/lg
   - 自定义阴影 card/card-hover/popover
   - transitionDuration fast/normal/slow
   - 动画 fade-in / pulse-dot

3. **重写 index.css**
   - base 层：字体平滑、滚动条美化
   - components 层：.btn(5变体3尺寸)、.card、.input、.badge(5语义)、.status-dot、.page-header、.skeleton
   - 所有组件含完整四态(hover/active/focus/disabled)

4. **建立 UI 组件层** `src/components/ui/`
   - Button.jsx、Card.jsx、Input.jsx、Badge.jsx
   - StatusDot.jsx、PageHeader.jsx、Skeleton.jsx、EmptyState.jsx
   - index.js 统一导出

5. **安装 lucide-react** 图标库

## 构建验证
`npx vite build` 通过，CSS 22KB / JS 203KB。

## 关键决策
- 不引入完整 UI 框架（如 Ant Design / shadcn），保持轻量，用 Tailwind 组件类自建
- 设计令牌走 Tailwind extend + CSS @layer components 双层落地
- 后续页面美化只使用 components/ui 中的组件，不直接堆砌原子类

## Phase 2：骨架美化（已完成）
1. **Toast 全局反馈系统** `components/ui/Toast.jsx`
   - ToastProvider + useToast hook，注入 main.jsx
   - success(3s自动消失) / error(需手动关闭) / info 三种
   - 右上角浮层，lucide 图标 + 左边框语义色
2. **ShellLayout 重构**
   - 侧边栏 w-72(288px)、主区 max-w-6xl 居中、p-8
   - 路由切换 fade-in 动效（key=pathname 触发重挂载）
3. **Sidebar 重构**
   - lucide 图标系统替换文本符号（Layers/Plus/ChevronDown/Right/Settings/Folder/FileText）
   - 工作区项 hover 时设置按钮渐显（opacity-0 group-hover:opacity-100）
   - 文件树缩进优化（depth * 12px）、空工作区提示
   - 用户头像改 brand-600 底色

## Phase 3：核心页面重做（已完成）
1. **Dashboard**：状态概览条 + 进度条(三色) + 4 个 StatCard + 骨架屏 + 刷新按钮
2. **Query**：PageHeader + 卡片输入 + ⌘Enter 快捷键 + 骨架屏 + 证据卡片 + 空状态
3. **Ingest**：PageHeader + 状态徽章 + 概览卡 + 分组操作卡(批量/实体/队列) + toast + 目录浏览器
4. **Workspace**：分组卡片表单(基础/Git) + toast + 目录浏览器 + 空状态

## 构建验证
最终 `npx vite build` 通过，CSS 26.7KB / JS 232KB。lint 零错误。dev server 运行在 5173。

## Phase 3 补充：剩余 7 页面重做（已完成）
5. **Entity**：PageHeader + 返回按钮 + 骨架屏 + Wiki 卡片 + 空状态
6. **Schema**：PageHeader + Schema 编辑卡(monospace) + 建议列表卡(采纳/忽略) + toast + 骨架屏
7. **Profile**：PageHeader + 用户标识卡 + 上下文/Wiki 双栏(只读 Badge) + AutoBlocks JSON 编辑卡 + toast
8. **UserSettings**：PageHeader + 底边线 Tab(带图标) + 基本设置卡 + toast
9. **LlmSettings**：PageHeader + 模型选择卡(select+Badge) + 温度滑块卡 + API Key 管理卡 + 测试结果卡 + toast
10. **WorkspaceSettings**：PageHeader + 底边线 Tab(4个带图标，横向滚动) + 复用子页面
11. **WorkspaceDoc**：PageHeader + 返回按钮 + 文件路径 + 骨架屏 + 错误状态 + 空状态

## Tab 组件统一
所有 Tab 页面（UserSettings / WorkspaceSettings）统一采用底边线样式：
- active: border-brand-600 + text-brand-700 + font-medium
- inactive: border-transparent + text-slate-600 + hover:text-slate-900
- 带 lucide 图标

## 关键决策
- 不引入完整 UI 框架，用 Tailwind 组件类自建
- 设计令牌走 Tailwind extend + CSS @layer components 双层落地
- 业务页面只用 components/ui 组件，不堆砌原子类
- 所有页面反馈从裸 message 文本升级为 Toast 系统
- 所有页面统一 PageHeader + Card 组件，消除散落的 h1 和裸 div 卡片

## 全部 11 个页面美化完成，Phase 1-3 全部落地。

## Phase 4：响应式适配（已完成）
1. **useMediaQuery hook** `hooks/useMediaQuery.js` — 监听 window.matchMedia 变化
2. **ShellLayout 响应式改造**
   - 桌面端(≥1024px)：侧边栏固定显示
   - 移动端(<1024px)：顶栏(汉堡菜单+Logo) + 抽屉式侧边栏 + 黑色遮罩层
   - 路由切换自动关闭抽屉
   - 主区内边距响应式：p-4(移动) / md:p-6 / lg:p-8
3. **Sidebar 适配** — min-h-screen → h-screen，适配 fixed 容器
4. **PageHeader 响应式** — 移动端标题+操作垂直堆叠(flex-col sm:flex-row)，标题 truncate
5. **page-title 字号响应式** — text-xl(移动) lg:text-2xl(桌面)
6. **触摸优化** — body 添加 -webkit-tap-highlight-color: transparent
7. **各页面 grid 已内置响应式** — Dashboard grid-cols-2 lg:grid-cols-4，双栏页 lg:grid-cols-2

构建：CSS 27.9KB / JS 234KB，lint 零错误。

## 关键决策
- 不引入完整 UI 框架，用 Tailwind 组件类自建
- 设计令牌走 Tailwind extend + CSS @layer components 双层落地
- 业务页面只用 components/ui 组件，不堆砌原子类
- 所有页面反馈从裸 message 文本升级为 Toast 系统
- 所有页面统一 PageHeader + Card 组件，消除散落的 h1 和裸 div 卡片
- 响应式断点：1024px（侧边栏固定/抽屉切换）

## Phase 1-4 全部完成。暗色模式作为可选项暂未实现。

## Docker 重新安装（18:51）
- 用户要求在 Docker 内重新安装项目
- 操作：`docker compose down` → `docker rmi` 删除旧镜像 → `docker compose up -d --build`
- 保留了数据卷（`langwiki_business-data`、`langwiki_langwiki-system`），仅重建镜像和容器
- 构建成功：前端 vite build（CSS 27.9KB / JS 234KB），后端 npm ci --omit=dev
- 容器 `langwiki` 运行正常，端口 3100，健康检查 HTTP 200
- 注意：`.env` 中 `DEEPSEEK_API_KEY` 为空，LLM 功能需配置后才能使用
- docker-compose.yml 的 `version: '3.9'` 已 obsolete（仅警告，不影响运行）

## 本地部署（20:31）
- 原因：Docker 容器内无法访问 Mac 本地目录进行测试
- 操作：`docker compose stop` 停止容器释放 3100 端口 → 直接 `node src/index.js` 本地启动
- 本地环境：Node v20.20.0，依赖已安装（node_modules 已存在）
- 数据目录：`/Users/jianglang/CodeBuddy/LangWIKI/LangWIKI/data/`（含 business/system/users）
- 服务进程：PID 94527，日志 `/tmp/langwiki-local.log`
- 健康检查 HTTP 200，访问地址 http://localhost:3100
- 注意：data/system/workspaces.json 里的工作区路径仍是容器内路径（/data/business、/app/data/...），需通过 UI 重新创建工作区指向 Mac 本地目录
- `.env` 中 `DEEPSEEK_API_KEY` 仍为空，LLM 功能需配置后才能使用
- 停止本地服务：`kill 94527`

## 新增 vLLM 自部署模型（20:33）
用户提供了 vLLM 部署的 Qwen3.6-35B-A3B-NVFP4 模型配置，要求集成到 LangWIKI。
- 模型预设名：`langai/Qwen3.6-35B-A3B-NVFP4`（provider=langai，走 openai_compat）
- 实际 model 名：`nvidia/Qwen3.6-35B-A3B-NVFP4`（发给 vLLM 的 model 字段）
- baseUrl：`http://111.231.100.113:3000/v1`（OpenAI 兼容接口）
- API Key：`sk-tcMsJZSsmOv9gj8nyXSEdvxE8msA0AQ1lSnZR2lnn6iKRu4b`（环境变量 LANGAI_API_KEY）
- 修改文件：src/llm/router.js（预设+provider映射+DEFAULT_BASES）、src/llm/config-store.js（ALL_ENV_KEYS）、frontend LlmSettings（PROVIDER_LABELS+ENV_KEY_LABELS）、.env、.env.example
- 连接测试成功，模型正确返回"连接成功"
- 新本地服务 PID: 95242
- **修复**：本地运行时需设置 `LANGWIKI_FRONTEND_DIST` 指向 `frontend/dist`，否则只显示纯文本欢迎页。已加入 `.env`，当前 PID: 96996

## Ingest 页面改造（21:18）
用户要求：1) ingest 页面移除"选择扫描目录"区块（目录在 manage 页面配置即可）；2) 把"按实体触发"改为"文件类型多选可增减"。
- 解释"按实体触发"：批量扫描处理所有子目录，按实体触发只处理指定子目录（高优先级）
- 移除了 Ingest 页面的"选择扫描目录"Card 和"按实体触发"Card
- 新增"文件类型筛选"Card：8 种类型可增减（PDF/Word/PowerPoint/Excel-CSV/图片OCR/邮件/纯文本/代码），点击切换选中/取消
- 后端改动：scanner.js 新增 FILE_TYPE_EXTENSIONS/FILE_TYPE_LABELS/resolveExtensions，listFiles 支持 fileTypes 过滤；orchestrator scheduleInitialScan+ingest 透传 fileTypes；routes/orchestrate.js 新增 GET /ingest/file-types，/ingest/initial 接收 fileTypes
- 前端 langwiki.js 新增 getIngestFileTypes()，triggerInitialIngest 增加 fileTypes 参数
- 新前端构建 CSS 28KB / JS 232KB，文件类型 API 正常返回 8 种类型

## manage 页面目录自动保存（21:39）
- Workspace/index.jsx 新增 selectAndSaveDir()：点击目录浏览器"选中"按钮时，不仅更新表单，还自动调用 updateWorkspace 保存 sourceDir 并 setActiveWorkspace 更新全局状态
- 效果：manage 页面选择目录后自动保存，ingest 页面的"扫描目录"概览也会显示该目录（useWorkspaceScope 读取最新的 sourceDir）

## 修复 sourceDir 跨页面不同步（21:48）
- 根因1：`setActiveWorkspace` 只存了 id/name/rootDir，没存 sourceDir → 切页面后丢失
- 根因2：`useWorkspaceScope` 只在 `id` 变化时 fetch，切 tab 时 id 不变不会重新获取
- 修复1：workspaceState.js 的 setActiveWorkspace 增加 sourceDir 和 git 字段
- 修复2：useWorkspaceScope.js 增加 onWorkspacesChanged 事件监听，收到变更事件后重新 listWorkspaces 并更新状态

## 清理旧容器工作区（21:51）
- 旧 workspaces.json 里 7 个工作区的 rootDir/sourceDir 全是容器路径（/data/business、/app/data/...），在 Mac 本地不存在导致操作失败
- 清理后只保留 local-ws：rootDir=/Users/.../data/workspaces/local-ws，sourceDir=/Users/.../data/business
- 后端 PATCH 校验 sourceDir 必须是已存在的绝对路径目录，之前选择目录保存失败就是因 rootDir 指向不存在的容器路径

## 修复 manage 页面 form 不同步（21:59，第5次修复）
- 根因：Workspace/index.jsx 第50行 useEffect 依赖 `[activeWorkspace?.id]`，保存后 activeWorkspace 对象变了但 id 没变，effect 不触发，form 不更新，UI 一直显示旧 sourceDir
- 修复：依赖改为 `[activeWorkspace]`，对象引用变化即触发 form 更新
- 端到端验证后端链路完全正确（GET→PATCH→GET 一致），问题纯在前端 form 同步

## 修复浏览器缓存问题（22:03）
- 用户强制刷新后仍无效，排查发现 express.static 和 sendFile 默认返回 Cache-Control: public, max-age=0 + ETag
- 修复：server.js 的 express.static 对 .html 文件设置 no-cache；SPA 路由 sendIndex 用 setHeader 设置 no-cache/no-store/must-revalidate
- 已重启服务（PID 6366 → 新 PID），所有 HTML 路径现在都返回 no-cache

## 最终修复 sourceDir 同步（22:37）
- 根因：目录浏览器的"选中"按钮有 `opacity-0 group-hover:opacity-100`（默认不可见），用户看不到按钮，点击的是目录名（进入子目录）而非"选中"
- 修复：改成始终可见的蓝底"选中此目录"按钮
- 交互逻辑改为：选中目录只更新输入框（setForm），不自动保存；用户确认后点"保存设置"统一提交（符合用户操作习惯）
- 移除了之前加的 selectAndSaveDir 自动保存逻辑和诊断 console.log
- Vite dev server (5173) + 后端 (3100) 均在运行
